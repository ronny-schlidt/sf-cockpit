# AGENTS.md

Read the root [CLAUDE.md](CLAUDE.md) before working on this repository. Then read only the documentation
relevant to the current task — see [docs/README.md](docs/README.md) for what each doc under `docs/` covers.

All project guidance (architecture, commands, conventions, pitfalls) lives in `CLAUDE.md` and `docs/`; this
file only points there so it doesn't drift out of sync with a second copy of the same rules.
